# Base image
FROM eclipse-temurin:17.0.3_7-jdk-jammy
# Where commands run
WORKDIR /app
# Maven dependency management
COPY .mvn/ .mvn
COPY mvnw pom.xml ./
RUN ./mvnw dependency:go-offline
# Copy source code
COPY src ./src
# Run Maven and SpringBoot
CMD ["./mvnw", "spring-boot:run"]
